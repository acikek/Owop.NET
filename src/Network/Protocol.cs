namespace Owop.Network;

public enum Opcode
{
    SetId,
    WorldUpdate,
    ChunkLoad,
    Teleport,
    SetRank,
    Captcha,
    SetPixelQuota,
    ChunkProtect,
    MaxPlayerCount,
    DonationTimer
}
